services:
  transitflow:
    build:
      context: .
      dockerfile: Dockerfile
    image: opentransitsoftwarefoundation/transitflow:latest
    container_name: transitflow
    ports:
      - "4000:4000"
    volumes:
      # Mount Docker-specific config (uses /app/data/gtfs.db for persistence)
      - ./config.docker.json:/app/config.json:ro
      # Persist SQLite database across container restarts
      - transitflow-data:/app/data
    environment:
      - TZ=UTC
    healthcheck:
      test: [ "CMD-SHELL", "wget -q -O /dev/null http://localhost:4000/healthz || exit 1" ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

volumes:
  transitflow-data:
    driver: local
